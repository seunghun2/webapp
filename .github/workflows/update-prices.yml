name: 실거래가 자동 업데이트

on:
  schedule:
    # 매일 한국시간 오전 11시 (UTC 2시)에 실행
    - cron: '0 2 * * *'
  workflow_dispatch:  # GitHub에서 수동 실행 가능

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: 실거래가 업데이트 API 호출
        run: |
          echo "🏠 실거래가 자동 업데이트 시작..."
          
          # ⚠️ 중요: Cloudflare Pages 배포 후 실제 URL로 변경하세요!
          # 예시: https://your-project.pages.dev/api/auto-update-all-prices
          #
          # 현재는 테스트용 URL (샌드박스)
          RESPONSE=$(curl -s -X POST \
            -H "Content-Type: application/json" \
            https://3000-iwhqnkbi44emm3qlpcntd-583b4d74.sandbox.novita.ai/api/auto-update-all-prices \
            -w "\n%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)
          
          echo "📊 응답 코드: $HTTP_CODE"
          echo "📄 응답 내용: $BODY"
          
          if [ "$HTTP_CODE" -eq 200 ]; then
            echo "✅ 실거래가 업데이트 성공!"
          else
            echo "❌ 실거래가 업데이트 실패!"
            exit 1
          fi
      
      - name: 업데이트 결과 알림
        if: always()
        run: |
          echo "🎉 자동 업데이트 작업 완료"
          echo "⏰ 실행 시간: $(date '+%Y-%m-%d %H:%M:%S KST')"
